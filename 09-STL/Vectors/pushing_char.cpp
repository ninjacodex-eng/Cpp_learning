#include <bits/stdc++.h>
using namespace std;
int main(){
        vector<char>u={'t','g','h'};
        u.push_back('j');
       cout<<"These are the values : ";
       for(char i : u){
                 cout<<i<<" ";  
       };
      return 0;
};
